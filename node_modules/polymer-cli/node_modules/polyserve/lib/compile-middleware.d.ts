/// <reference types="lru-cache" />
/// <reference types="express" />
import { RequestHandler } from 'express';
import * as LRU from 'lru-cache';
export declare const isPolyfill: RegExp;
export declare const babelCompileCache: LRU.Cache<string>;
export declare const getCompileCacheKey: (requestPath: string, body: string, options: {
    compileTarget: string;
    transformModules: boolean;
}) => string;
export declare function babelCompile(compile: 'always' | 'never' | 'auto', moduleResolution: 'none' | 'node', rootDir: string, packageName: string, componentUrl: string, componentDir: string): RequestHandler;
