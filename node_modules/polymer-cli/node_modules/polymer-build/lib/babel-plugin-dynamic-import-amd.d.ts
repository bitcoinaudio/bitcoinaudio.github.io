import { NodePath } from '@babel/traverse';
import { Program } from 'babel-types';
/**
 * Rewrites dynamic import() calls to AMD async require() calls.
 */
export declare const dynamicImportAmd: {
    inherits: any;
    visitor: {
        Program(path: NodePath<Program>): void;
    };
};
